version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./database:/app/database
      - ./segments:/app/segments
    environment:
      - FLASK_ENV=development
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=.
