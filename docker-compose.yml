version: '3.8'

services:
  web:
    image: kr2mdevs/traitement-signal:latest
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./database:/app/database
      - ./segments:/app/segments
    environment:
      - FLASK_ENV=development
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=.
